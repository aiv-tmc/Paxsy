#include(console.hp);

__begin {
    $num : int = 0;

    console::output("Enter a number: ");
    console::input($nun);

    if($num % 2 == 0) console::output("%i even number", $num);
    else console::output("%i odd number", $num);
    return(0);
}
