#include(base.hpi);

__start : unsig 2int1 {
    $num : int = 0;

    _print("Enter a number: ");

    syscall::read($num);

    if ($num % 2 == 0) { _print($num, "even number"); }
    else { _print($num, "odd number"); }
    return(2x0);
}
