#include "commands.h"
#include <stdio.h>

/*
 * Run the project's test suite (placeholder).
 * project_path: path to project root (NULL for current directory).
 */
int cmd_test(const char *project_path) {
    (void)project_path; /* unused */
    printf("Test command not yet implemented.\n");
    return 0;
}
